# Optional — api.vancam.ai key for higher rate limits.
# Get a free personal key from your account page at https://vancam.ai
# Unset defaults to a shared anonymous key (1 req/s, 500/month, pooled across all anonymous callers).
VANCAM_API_KEY=
